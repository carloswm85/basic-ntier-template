@model BasicNtierTemplate.Data.Model.Student

@{
    ViewData["Title"] = "Delete Student";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow rounded-3 border">
                <div class="card-header border-0 bg-danger text-white">
                    <h4 class="mb-0">Delete Student</h4>
                </div>
                <div class="card-body">
                    <h5 class="text-danger mb-4">Are you sure you want to delete this student?</h5>

                    <dl class="row">
                        <dt class="col-sm-4 fw-semibold text-secondary">
                            @Html.DisplayNameFor(model => model.LastName)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.LastName)
                        </dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">
                            @Html.DisplayNameFor(model => model.FirstMidName)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.FirstMidName)
                        </dd>

                        <dt class="col-sm-4 fw-semibold text-secondary">
                            @Html.DisplayNameFor(model => model.EnrollmentDate)
                        </dt>
                        <dd class="col-sm-8">
                            @Html.DisplayFor(model => model.EnrollmentDate)
                        </dd>
                    </dl>

                    <hr />

                    <form asp-controller="contoso" asp-action="delete" asp-route-id="@Model.Id" class="mt-4">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-between">
                            <a asp-controller="contoso" asp-action="students" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left-circle"></i> Back to List
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash"></i> Confirm Delete
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
